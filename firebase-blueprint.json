{
  "entities": {
    "users": {
      "title": "users",
      "description": "User account profiles with name, department, school, email, and activation status",
      "type": "object",
      "properties": {
        "uid": { "type": "string" },
        "email": { "type": "string" },
        "name": { "type": "string" },
        "department": { "type": "string" },
        "school": { "type": "string" },
        "isActivated": { "type": "boolean" },
        "activationToken": { "type": "string" },
        "activatedAt": { "type": "string" },
        "createdAt": { "type": "string" },
        "updatedAt": { "type": "string" }
      },
      "required": ["uid", "email", "name", "department", "school"]
    },
    "userProgress": {
      "title": "userProgress",
      "description": "User calculus mastery quiz progress, answer history, and custom questions",
      "type": "object",
      "properties": {
        "uid": { "type": "string" },
        "progress": { "type": "object" },
        "resultsHistory": { "type": "array" },
        "customQuestions": { "type": "array" },
        "settings": { "type": "object" },
        "updatedAt": { "type": "string" }
      },
      "required": ["uid"]
    },
    "tokens": {
      "title": "tokens",
      "description": "Support activation tokens generated by admin (jentlecasper014@gmail.com) to unlock user hints, answers, and detailed solution images",
      "type": "object",
      "properties": {
        "code": { "type": "string" },
        "isUsed": { "type": "boolean" },
        "usedByUid": { "type": "string" },
        "usedByEmail": { "type": "string" },
        "createdAt": { "type": "string" },
        "usedAt": { "type": "string" }
      },
      "required": ["code", "isUsed"]
    }
  }
}
